{
  "name": "eyes-sdk-javascript",
  "private": true,
  "scripts": {
    "install": "lerna bootstrap --hoist",
    "bootstrap": "lerna bootstrap --hoist",
    "test": "lerna run test",
    "test:unit": "lerna run test:unit"
  },
  "devDependencies": {
    "lerna": "^3.10.5"
  }
}
